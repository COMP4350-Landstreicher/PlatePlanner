CREATE DATABASE PlatePlanner;

USE PlatePlanner;

/*CREATE TABLE Users (
	email varchar(20) NOT NULL,
	user_name varchar(20) NOT NULL,
	salt varchar(40) NOT NULL,
	password varchar(40) NOT NULL,
	first_name varchar(20) NOT NULL,
	last_name varchar(20) NOT NULL,
	PRIMARY KEY (email)
);*/
