{
  "name": "PlatePlanner",
  "version": "0.0.1",
  "description": "PlatePlanner Main Application",
  "author": "Landstreicher",
  "main": "PlatePlannerServer.js",
  "scripts": {
    "frontend": "cd client && npm install && npm start",
    "backend": "cd server && node PlatePlannerServer.js",
    "start": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^7.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.1"
  }
}
